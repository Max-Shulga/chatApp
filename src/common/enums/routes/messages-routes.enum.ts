export enum MessagesRoutesEnum {
  BasePrefix = "messages",
  SendMessage = "send-message",
  GetMessagesByChatId = "get-messages/:chatId",
  Subscribe = "subscribe",
  Unsubscribe = "unsubscribe",
}
